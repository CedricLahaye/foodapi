{% extends 'base.html.twig' %}

{% block title %}New Product{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center mt-3">Create new Product</h1>

        <div class="text-center">{{ include('products/_form.html.twig') }}</div>

        <a href="{{ path('app_products_index') }}" class="btn btn-info">back to list</a>
    </div>
{% endblock %}
